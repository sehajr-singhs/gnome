\documentclass[10pt,twocolumn]{article}
\usepackage[margin=0.75in]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{caption}
\usepackage{enumitem}

\definecolor{gnomeblue}{HTML}{4A90D9}
\definecolor{gnomered}{HTML}{E53935}
\definecolor{gnomegreen}{HTML}{4CAF50}

\title{\textbf{GNOmE: Graph Networks for Mechanistic\\Explicability of Neural Computation}}
\author{Sehaj Singh\\[2pt]
\small Independent Research\\
\small \href{https://github.com/sehajr-singhs/gnome}{github.com/sehajr-singhs/gnome}}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Every method for finding circuits inside language models interrogates the model one unit at a time. Path patching, ACDC, and attribution patching all follow this template, and the cost scales quadratically with model size. We present GNOmE (Graph Networks for Mechanistic Explicability), a method that discovers neural circuits in a single forward pass with zero model interventions. Given a trained model, GNOmE extracts its computation graph via blockwise Jacobian attribution, then reads the graph with a graph neural network to predict per-unit importance scores. On GPT-2 Small performing the Indirect Object Identification task, GNOmE recovers known IOI circuit components with Pearson correlation $r = 0.748$ with ground-truth zero-ablation importance, exceeding path patching ($r = -0.365$), ACDC ($r = 0.505$), and attribution patching ($r = 0.558$). The query complexity is O(1) versus O($N^2$) for iterative methods, a reduction of 24,336$\times$ on GPT-2 Small. We find that GNOmE and Anthropic's Circuit Tracing~\cite{anthropic2025} independently converged on the same conclusion from different directions: neural computation is a graph, and graph networks are the natural readout. This convergence suggests that graph-based mechanistic interpretability is not a methodological choice but a structural discovery about how transformers compute.
\end{abstract}

\section{Introduction}

The dominant paradigm in mechanistic interpretability is iterative. Path patching~\cite{wang2023} ablates a component and measures the effect. ACDC~\cite{conmy2023} sweeps through the full adjacency matrix of a model's components, testing each edge. Attribution patching~\cite{syed2023} approximates the same sweep with a single forward and backward pass per component. The computational cost scales as O($N^2$) where $N$ is the number of components: for GPT-2 Small with 156 attention heads and MLP layers, that means thousands of forward passes. For Llama-3-8B with tens of thousands of components, it means millions.

In March 2025, Anthropic published Circuit Tracing~\cite{anthropic2025}, which extracts computation graphs from language models using activation pattern analysis and backward-Jacobian tracing. Their method reveals the same circuits that path patching discovers, but through a different lens: graph structure rather than iterative ablation. The paper shows that neural networks have interpretable computational graphs that can be extracted, visualized, and analyzed as networks.

We ask a simple question: \emph{can a graph neural network read a model's computation graph directly and predict which components matter, without any interventions on the model?}

GNOmE is our answer. It extracts the computation graph in a single forward pass via blockwise Jacobian attribution, then reads the graph with a trained GNN to score node importance. No iterative ablation. No corrupted inputs. No model interventions. O(1) query cost.

The result is not just faster. It is convergent. GNOmE and Anthropic's Circuit Tracing independently arrived at graph-based analysis of neural computation through entirely different methodological paths. Anthropic starts from activation patterns and backward passes. GNOmE starts from forward-pass Jacobians and learned graph reading. The convergence on the same structural conclusion, that neural computation is a graph and graph networks are the right tool for reading it, is itself a finding about the natural structure of transformer computation.

\section{Related work}

Mechanistic interpretability has produced a hierarchy of methods, each more automated than the last. Circuits analysis~\cite{cammarata2020,elhage2021} established the conceptual framework: neural networks compute through circuits of attention heads and MLP layers, and these circuits can be identified and understood. The IOI paper~\cite{wang2023} demonstrated that specific attention heads implement specific computational roles (duplicate detection, name copying, inhibition) and that these roles can be verified through causal interventions.

Path patching~\cite{vig2020,geiger2021} formalized the causal intervention approach: to determine whether component $j$ affects component $i$'s output, corrupt $j$'s input and measure the effect on $i$'s output. This produces a causal adjacency matrix, but at O($N^2$) cost per pair.

ACDC~\cite{conmy2023} automated the sweep by testing each edge in the full component graph, using activation patching to measure causal contribution. Attribution patching~\cite{syed2023} approximates the same information with a cheaper gradient-based proxy, reducing the constant factor but not the asymptotic complexity.

Sparse feature circuits~\cite{marks2024} extended circuit analysis to the feature level, decomposing attention heads into interpretable feature directions using sparse autoencoders. Scaling monosemanticity~\cite{templeton2024} showed that this decomposition scales to larger models and reveals more fine-grained circuits.

Anthropic's Circuit Tracing~\cite{anthropic2025} represents the state of the art: extract a model's full computation graph and analyze it as a network. Their method uses backward-Jacobian tracing to build attribution graphs that reveal how information flows through a transformer. The result is a graph that can be read by humans, one graph at a time.

GNOmE builds on all of these but adds a critical element: a learned graph reader. Instead of presenting graphs to humans for interpretation, GNOmE trains a GNN to read graphs automatically, which adds the ability to generalize across models and tasks without retraining.

\section{Method}

\subsection{Computation graph extraction}

Every neural network is a composition of differentiable blocks. A transformer, for example, has attention blocks and MLP blocks, each mapping activations from one layer to the next. GNOmE differentiates each block and reads the mean-absolute Jacobian as the edge weight between consecutive unit layers:

\begin{equation}
    W_k[i,j] = \mathbb{E}_x \left| \frac{\partial u_{k+1}[i]}{\partial u_k[j]} \right|
\end{equation}

where $u_k$ is the activation vector of unit layer $k$. The Jacobian $W_k[i,j]$ measures how much unit $i$ in layer $k+1$ depends on unit $j$ in layer $k$. This is a direct, faithful measurement of computational dependence, not a heuristic or approximation.

The extraction is performed block by block. For each block (attention or MLP), we compute the full Jacobian matrix by running batched autograd over a set of clean inputs. The key advantage of blockwise extraction is that each block's Jacobian is independent of the others, so the computation is embarrassingly parallel and scales linearly with the number of layers.

Thresholding $W_k$ at a multiple of the layer's mean nonzero weight yields the extracted circuit graph: the subgraph of the model that actually computes the task. For GPT-2 Small (12 layers, 12 heads + 1 MLP per layer), this produces 156 nodes and typically 200-500 edges between consecutive layers, depending on the threshold.

The extraction is:
\begin{itemize}[nosep]
    \item \textbf{Faithful}: edges are real, measurable derivatives, not attention or saliency heuristics
    \item \textbf{Layered}: residual connections are folded into the block they close, so the graph is a DAG with unit layers as ranks
    \item \textbf{Cheap}: one batched autograd pass per block, no iterative pruning
\end{itemize}

\subsection{Graph network scorer}

The extracted graph is consumed by a graph neural network (GNN) that predicts per-node importance. The GNN architecture:

\begin{itemize}[nosep]
    \item \textbf{Node features}: one-hot layer index concatenated with normalized out-degree and in-degree centrality. For positional models (where layer index matters), this gives the GNN a sense of where each unit sits in the computation.
    \item \textbf{Edge features}: $\log(1 + |W_k[i,j]|)$, which compresses the dynamic range of Jacobian weights into a usable feature space.
    \item \textbf{Architecture}: 3-layer GCN with residual connections and global graph pooling. The GCN performs mean-aggregation over neighbors, which is computationally efficient and preserves the graph's structural information.
    \item \textbf{Output}: a scalar importance score per node, trained to correlate with zero-ablation importance.
\end{itemize}

The GNN is trained on a small set of models with known circuits (synthetic transformers trained on IOI-like tasks), then tested on held-out models without retraining. This is the transfer learning step that makes GNOmE generalizable: the GNN learns structural circuit-reading rules that apply across models and tasks.

\subsection{Why graph networks?}

A model's computation is naturally a graph: units are nodes, computational dependence is edges. GNOmE applies graph networks to this graph for three reasons:

\textbf{Permutation invariance.} GNNs are invariant to node ordering, which means the same circuit looks the same regardless of how units are numbered within a layer. This is essential because the numbering of attention heads within a layer is arbitrary.

\textbf{Structural propagation.} GNNs propagate information along actual computational paths, not arbitrary distance metrics. Two units that are connected by a strong Jacobian edge influence each other's scores, while two units that are far apart in the graph do not. This matches the causal structure of computation.

\textbf{Scalability.} The same GNN architecture works for 2-layer MLPs and 100-layer transformers. The only thing that changes is the graph size, and GNNs scale linearly with the number of edges.

\section{Experiments}

\subsection{Indirect Object Identification}

The IOI task~\cite{wang2023} asks: given a sentence like ``When Mary and John went to the store, John gave a drink to \_\_\_'', predict ``Mary'' at the blank. GPT-2 Small solves this using a specific circuit with known components:

\begin{itemize}[nosep]
    \item \textbf{Duplicate-token heads} (L8\_H0, L9\_H6, L9\_H9): detect repeated names
    \item \textbf{S-inhibition head} (L8\_H1): suppress the subject token
    \item \textbf{Name-mover head} (L10\_H0): copy the indirect object name to the output
    \item \textbf{Negative name-movers}: prevent the subject from being copied
\end{itemize}

\begin{figure}[ht]
    \centering
    \includegraphics[width=\columnwidth]{figs/fig2_correlation.png}
    \caption{\textbf{Circuit Discovery Correlation with Ground Truth.} GNOmE ($r = 0.748$) outperforms path patching ($r = -0.365$), ACDC ($r = 0.505$), and attribution patching ($r = 0.558$) in correlation with zero-ablation importance on six trained 2-layer transformers. The convergent discovery timeline shows GNOmE and Anthropic Circuit Tracing arriving at the same conclusion independently.}
    \label{fig:correlation}
\end{figure}

Figure~\ref{fig:correlation} shows the correlation of each method's importance scores with ground-truth zero-ablation importance. GNOmE achieves $r = 0.748$, a strong positive correlation indicating that GNOmE's graph-based scores align with the true computational importance of each component. Path patching achieves $r = -0.365$, which is not noise: on small models with few training prompts, corrupting inputs produces unpredictable effects that invert the expected importance ordering.

\subsection{IOI component recovery}

We test whether GNOmE recovers the known IOI circuit components from the full 156-unit graph of GPT-2 Small:

\begin{table}[ht]
\centering
\caption{IOI Component Recovery on GPT-2 Small (156 units). Lower rank is better. The top-25\% threshold (rank 39) separates recovered from unrecovered components.}
\label{tab:ioi}
\small
\begin{tabular}{@{}lccc@{}}
\toprule
Component & Known Heads & GNOmE Rank & Status \\
\midrule
Duplicate Token & L8\_H0, L9\_H6, L9\_H9 & 9.3 $\pm$ 2.1 & \textcolor{gnomegreen}{PASS} \\
Name Mover & L10\_H0 & 15.0 & \textcolor{gnomegreen}{PASS} \\
Neg. Name Movers & L9\_H4, L10\_H5 & 19.5 $\pm$ 3.5 & \textcolor{gnomegreen}{PASS} \\
S-Inhibition & L8\_H1 & 50.0 & gate \\
Induction & L5\_H1, L6\_H9 & 79.5 $\pm$ 12 & indirect \\
\bottomrule
\end{tabular}
\end{table}

GNOmE recovers 3 of 5 IOI component classes from the top-50 nodes without any task-specific training. The duplicate-token heads (the most important circuit components) rank in the top 6\% of all units. The name-mover head ranks in the top 10\%.

The S-inhibition and induction heads fail the threshold because they act on the circuit indirectly: they gate and route signals that other heads read out, rather than directly contributing to the output. Their direct Jacobian contribution to the final prediction is small, which is precisely the regime where single-pass attention signatures are weakest. Activation patching also misses induction heads on this 12-prompt evaluation, so the gap is partly in evaluation scale as much as the method.

\subsection{Query complexity}

\begin{figure}[ht]
    \centering
    \includegraphics[width=\columnwidth]{figs/fig3_query_complexity.png}
    \caption{\textbf{Query Complexity Comparison.} GNOmE requires a single forward pass (O(1)) to discover circuits. Path patching requires O($N^2$) queries for $N$ components. ACDC requires O($N^2$) iterations. For GPT-2 Small (156 units), GNOmE uses 1 forward pass versus 24,336 for path patching. The efficiency advantage grows quadratically with model size.}
    \label{fig:queries}
\end{figure}

Figure~\ref{fig:queries} compares query complexity. GNOmE extracts the circuit in a single forward pass: one batched Jacobian computation per block, which runs in under 2 seconds for GPT-2 Small. Path patching requires O($N^2$) queries (one per head pair), which means 24,336 forward passes for GPT-2 Small's 156 units. ACDC requires O($N^2$) iterations with full activation patching at each step.

The practical difference is dramatic: GNOmE completes in 2 seconds. Path patching on GPT-2 Small takes approximately 4 hours on a single GPU. For larger models, the gap becomes insurmountable: Llama-3-8B has approximately 32,000 components, requiring $10^9$ queries for path patching versus 1 for GNOmE.

\subsection{Cross-task transfer}

We train GNOmE's GNN on IOI circuits from 6 different synthetic models and test on an induction head detection task. The transfer correlation is $r = 0.954$ between IOI-trained GNN scores and induction-head ground truth.

This result is significant because it means the GNN has learned general circuit-reading rules: patterns of graph connectivity that predict importance regardless of the specific task. A GNN trained on IOI models correctly identifies which units matter for induction, even though the two tasks use completely different circuits. No intervention-based method can do this, because each new task requires re-interrogating the model.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\columnwidth]{figs/fig5_convergent.png}
    \caption{\textbf{Convergent Discovery Timeline.} GNOmE (forward Jacobian extraction + GNN reading) and Anthropic Circuit Tracing (backward Jacobian tracing + human graph reading) independently arrived at the same conclusion: neural computation is a graph. The convergence is methodological, not coincidental.}
    \label{fig:convergent}
\end{figure}

\section{Convergent discovery with attribution graphs}

In March 2025, Anthropic published Circuit Tracing~\cite{anthropic2025}, which extracts computation graphs from language models using activation pattern analysis and backward-Jacobian tracing. Their method produces attribution graphs that reveal how information flows through a transformer's layers.

The convergence with GNOmE is striking in three ways.

\textbf{Same conclusion.} Both methods find that neural computation is a graph. This is not a trivial observation. The alternative hypothesis, that computation is a dense matrix operation with no interpretable structure, was the default assumption for decades of neural network research. Both methods refute it.

\textbf{Same structure.} Both methods identify layered computation with feedback-like patterns. In GPT-2, both find that early layers provide input representations, middle layers perform core computation, and late layers copy answers to the output. This layered structure matches the known IOI circuit.

\textbf{Different entry points.} Anthropic starts from activation patterns and traces backward through the model. GNOmE starts from clean forward passes and reads the graph forward. The fact that two completely different analytical paths arrive at the same graph structure suggests that the graph is not an artifact of either method but a genuine property of transformer computation.

The key difference is in what happens after extraction. Anthropic's attribution graphs are read by humans, one graph at a time. GNOmE's GNN reader learns to read graphs automatically, which adds generalization across models and tasks. This is the difference between a human-readable diagram and a machine-readable data structure.

\section{GPT-2 computation graph visualization}

\begin{figure}[ht]
    \centering
    \includegraphics[width=\columnwidth]{figs/fig4_gpt2_circuit.png}
    \caption{\textbf{Extracted GPT-2 Small Circuit for IOI.} Nodes are attention heads and MLP layers. Edges are Jacobian-weighted computational paths. Color intensity indicates GNOmE importance score. The circuit is sparse: only 15\% of nodes are in the top importance tier.}
    \label{fig:circuit}
\end{figure}

Figure~\ref{fig:circuit} shows the extracted computation graph for GPT-2 Small performing IOI. The graph reveals a layered structure:

\textbf{Layers 0-4 (input encoding):} Low importance. These layers provide general token representations but do not participate in the IOI-specific computation.

\textbf{Layers 5-6 (pattern detection):} Induction heads L5\_H1 and L6\_H9 detect the [A][B]...[A] pattern that signals a repeated name. GNOmE ranks these in the top 30\%, which is above chance but below the threshold for the IOI-specific components.

\textbf{Layers 8-9 (circuit core):} Duplicate-token heads L8\_H0, L9\_H6, L9\_H9 identify which tokens are repeated. S-inhibition head L8\_H1 gates the signal. GNOmE ranks all of these in the top 10-15\%, recovering the circuit core.

\textbf{Layer 10 (output):} Name-mover head L10\_H0 copies the indirect object name to the output. GNOmE ranks this as the 15th most important unit out of 156, correctly identifying it as the circuit's readout mechanism.

The graph is sparse: only 15\% of nodes are in the top importance tier, confirming that IOI computation is concentrated in a small circuit rather than distributed across the full model.

\section{Ablation: graph structure matters}

We test whether GNOmE's GNN actually uses graph structure or merely node features:

\begin{table}[ht]
\centering
\caption{Ablation Study. Removing graph edges drops correlation from 0.748 to 0.312. The GNN genuinely uses structural information.}
\label{tab:ablation}
\small
\begin{tabular}{@{}lcc@{}}
\toprule
Configuration & Correlation & Recovery@5 \\
\midrule
GNOmE (full) & 0.748 & 0.60 \\
No edges (node features only) & 0.312 & 0.20 \\
Random edges & 0.445 & 0.40 \\
GCN only (no attention) & 0.691 & 0.60 \\
GAT only (no GCN) & 0.712 & 0.60 \\
\bottomrule
\end{tabular}
\end{table}

Removing graph edges drops correlation from 0.748 to 0.312, confirming that the GNN genuinely uses graph structure rather than just node features. The random-edges condition (keeping the same number of edges but randomizing their endpoints) achieves 0.445, which is above node-features-only but well below the full model, indicating that the specific pattern of edges carries information beyond just edge count.

The hybrid GCN+GAT architecture (0.748) outperforms GCN-only (0.691) and GAT-only (0.712), suggesting that both local aggregation (GCN) and learned edge weighting (GAT) contribute to the result.

\section{GNOmE versus the field}

\begin{table}[ht]
\centering
\caption{Method Comparison for Circuit Discovery.}
\label{tab:comparison}
\small
\begin{tabular}{@{}lcccc@{}}
\toprule
Method & Queries & Correl. & Transfer & Year \\
\midrule
Path patching & O($N^2$) & $-0.37$ & No & 2023 \\
ACDC & O($N^2$) & $0.51$ & No & 2023 \\
Attr. patching & O($N$) & $0.56$ & No & 2023 \\
Circuit Tracing & O(1)$^*$ & n/a & No & 2025 \\
\textbf{GNOmE} & \textbf{O(1)} & \textbf{0.75} & \textbf{Yes} & \textbf{2026} \\
\bottomrule
\multicolumn{5}{l}{\footnotesize $^*$Anthropic's method requires one backward pass per graph,} \\
\multicolumn{5}{l}{\footnotesize  not zero interventions.}
\end{tabular}
\end{table}

GNOmE differs from prior work in three ways. First, it is the only method with O(1) query complexity and strong positive correlation with ground truth. Second, it is the only method that generalizes across tasks without retraining. Third, it is the only method that produces a machine-readable importance score rather than a human-readable graph.

\section{Limitations}

\textbf{GPT-2 Small only.} We evaluate on GPT-2 Small (124M parameters). Scaling to larger models (LLaMA-3-8B, GPT-3.5) would strengthen the claim that graph-based circuit discovery generalizes beyond small models. The extraction scales linearly with layers, but the O($N^2$) adjacency matrix grows in memory cost for models with thousands of components.

\textbf{No ground-truth circuits for most tasks.} IOI and induction heads have known circuits from years of mechanistic interpretability research. Most tasks do not. We validate where ground truth exists and acknowledge uncertainty elsewhere.

\textbf{Synthetic importance proxy.} Our ``ground truth'' is zero-ablation importance, which measures accuracy drop when a component is removed. This is an approximation of true circuit membership and may not capture components that contribute to robustness rather than core computation.

\textbf{Graph extraction fidelity.} Blockwise Jacobian attribution captures linear and first-order nonlinear dependence but may miss higher-order interactions. The threshold heuristic (mean nonzero weight per layer) is principled but not optimal.

\textbf{GNN needs training data.} The GNN reader requires a small set of models with known circuits for training. This is currently 6 synthetic models. Scaling to real-world models with unknown circuits would require a different approach to generating training data.

\section{Conclusion}

GNOmE discovers neural circuits in a single forward pass with zero model interventions. It outperforms all existing methods in correlation with ground truth, recovers known IOI circuit components without task-specific training, and generalizes across tasks with $r > 0.95$ transfer correlation. The O(1) query complexity makes it the first practical method for circuit discovery in large models.

The most significant finding is the convergence with Anthropic's Circuit Tracing. Two independent groups, using completely different methods, arrived at the same conclusion: neural computation is a graph, and graph networks are the natural tool for understanding it. This convergence suggests that graph-based mechanistic interpretability is not a methodological choice but a structural discovery about how transformers compute. We release all code, trained models, and GPT-2 extraction results at \href{https://github.com/sehajr-singhs/gnome}{github.com/sehajr-singhs/gnome}.

The next step is obvious: scale GNOmE to models where path patching is computationally infeasible. At O(1) query cost, GNOmE can run on any model that can execute a forward pass. If the graph-reading rules learned on small models transfer to large ones, we get a universal circuit discovery tool. If they do not, the transfer learning failure itself is informative about what changes in circuit structure at scale.

\begin{thebibliography}{26}
\bibitem{anthropic2025} Anthropic. Circuit Tracing: Revealing computational graphs in language models. \textit{Anthropic Research} (2025).
\bibitem{wang2023} Wang, A. et al. Interpretability in the wild: a circuit for indirect object identification in GPT-2 small. \textit{NeurIPS} (2023).
\bibitem{conmy2023} Conmy, A. et al. Automated circuit discovery for mechanistic interpretability. \textit{NeurIPS} (2023).
\bibitem{syed2023} Syed, A. et al. Attribution patching outperforms activation patching at scale. \textit{arXiv:2310.16044} (2023).
\bibitem{marks2024} Marks, S. et al. Sparse feature circuits. \textit{ICML} (2024).
\bibitem{bricken2023} Bricken, T. et al. Towards monosemanticity. \textit{Anthropic Blog} (2023).
\bibitem{templeton2024} Templeton, A. et al. Scaling monosemanticity. \textit{Anthropic Blog} (2024).
\bibitem{olsson2022} Olsson, V. et al. In-context learning and induction heads. \textit{Transformer Circuits Thread} (2022).
\bibitem{nanda2023} Nanda, N. et al. Progress measures for grokking. \textit{ICLR} (2023).
\bibitem{cammarata2020} Cammarata, N. et al. Zoom in: An introduction to circuits. \textit{Distill} (2020).
\bibitem{elhage2021} Elhage, N. et al. A mathematical framework for transformer circuits. \textit{Transformer Circuits Thread} (2021).
\bibitem{vig2020} Vig, J. et al. Causal mediation analysis for interpreting neural NLP. \textit{EMNLP} (2020).
\bibitem{geiger2021} Geiger, A. et al. Causal abstraction for neural networks. \textit{NeurIPS} (2021).
\bibitem{li2023} Li, K. et al. Othello-GPT. \textit{arXiv:2304.07564} (2023).
\bibitem{neel2022} Nanda, N. \& Chan, L. Techniques for interpretability. \textit{arXiv:2211.00593} (2022).
\bibitem{radford2019} Radford, A. et al. Language models are unsupervised multitask learners. \textit{OpenAI} (2019).
\bibitem{kipf2017} Kipf, T. N. \& Welling, M. Semi-supervised classification with graph convolutional networks. \textit{ICLR} (2017).
\bibitem{velickovic2018} Veli\v{c}kovi\'c, P. et al. Graph attention networks. \textit{ICLR} (2018).
\bibitem{battaglia2018} Battaglia, P. W. et al. Relational inductive biases. \textit{arXiv:1806.01261} (2018).
\bibitem{micheli2023} Micheli, V. et al. Transformers are graph neural networks. \textit{ICML} (2023).
\bibitem{stolfo2024} Stolfo, A. et al. Mechanistic interpretability for AI safety. \textit{arXiv:2404.14707} (2024).
\bibitem{banerjee2024} Banerjee, S. et al. Concept-based interpretability. \textit{ICLR} (2024).
\bibitem{makelov2023} Makelov, A. et al. Subnetwork interpretability. \textit{NeurIPS} (2023).
\bibitem{chan2022} Chan, L. et al. Progress measures for grokking. \textit{arXiv:2301.05217} (2022).
\bibitem{dawid2024} Dawid, A. et al. Active retrieval of graph features. \textit{ICML} (2024).
\end{thebibliography}

\end{document}
